void main() {
  List<String> names = ["Milenna", "Antonie", "Rafael", "Michael"];
  print(names.map((e) => e + e));
  List<int> nums = [1, 2, 3, 4, 5];
  print(nums.map((e) => e * e));
  //Map goes through each element of the list and applied a function on them.
  
}
